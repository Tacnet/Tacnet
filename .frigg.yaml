tasks:
  - tox -e flake8
  - tox -e isort
  - tox -e tests
  - tox -e coverage

verbose_tasks:
  - tox -e coverage

comment: false

services:
  - postgresql

coverage:
  path: coverage.xml
  parser: python
